name: abgabe_http_api
version: 1.0.0
description: OpenAPI API client
publish_to: none

environment:
    sdk: ">=2.3.0 <3.0.0"

dependencies:
    dio: ^3.0.9
    built_value: ^7.1.0
    built_collection: ^4.3.2

dev_dependencies:
    built_value_generator: ^7.1.0
    build_runner: ^1.10.0
    test: ^1.14.7
