# Bolt's Journal

## 2024-05-22 - [O(n²) String Concatenation in Utilities]
**Learning:** Found O(n²) string concatenation in `randomIDString` utility function using `+=` in a loop.
**Action:** Always use `StringBuffer` for constructing strings in loops, especially in utility functions that might be used frequently.
