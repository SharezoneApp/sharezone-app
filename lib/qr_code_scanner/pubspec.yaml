name: qr_code_scanner
description: A wrapper around a package to scan qr codes.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  mobile_scanner: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  golden_toolkit: ^0.13.0
  mockito: ^5.1.0
  build_runner: ^2.1.8

flutter:
  uses-material-design: true
